import React from 'react';
import ReactDom from 'react-dom';

export default class TaskAddInput extends React.Component {
    render () {
        return (
            <div>
                list
            </div>
        );
    }
}